version: '2'

tasks:
  build:
    cmds:
      - go build -o dist/banala

  run:
    deps: [build]
    cmds:
      - dist/banala

  deploy:
    deps: [build]
    cmds:
      - rsync -arvz --exclude 'db.bdg' dist/*  root@mail.protecmail.com:/root/bin/
