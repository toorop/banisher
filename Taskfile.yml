version: '2'

tasks:
  build:
    cmds:
      - go build -o bin/banala

  run:
    deps: [build]
    cmds:
      - bin/banala

  deploy:
    deps: [build]
    cmds:
      - rsync -arvz bin/* root@mail.protecmail.com:/root/bin/


